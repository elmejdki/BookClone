<div class="page-wrapper">
  <div class="form-container sign-up">
    <h2 class="title is-3">Forgot your password?</h2>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field is-horizontal">
        <div class="field-body">
          <div class="field">
            <p class="control is-expanded has-icons-left">
              <%= f.text_field :email, autofocus: true, autocomplete: "email", class: 'input is-small', placeholder: 'Enter your email' %>
              <span class="icon is-left">
                <i class="fas fa-envelope"></i>
              </span>
            </p>
          </div>
        </div>
      </div>

      <div class="actions">
        <%= f.submit "Send me reset password instructions", class: 'button is-success' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
